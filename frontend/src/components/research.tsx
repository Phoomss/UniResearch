import Link from "next/link";
import { ArchiveTab, Status } from "./ui";
import type { ResearchViewModel } from "@/src/features/research/adapters";

function statusTone(status:ResearchViewModel["status"]):"approved"|"review"|"revision"|"error" { return status==="approved"?"approved":status==="revision_needed"?"revision":status==="rejected"?"error":"review"; }
function statusLabel(item:ResearchViewModel){ return item.status==="approved"?"เผยแพร่แล้ว":item.status==="pending"?"รอตรวจสอบ":item.status==="rejected"?"ไม่ผ่านการตรวจสอบ":item.status==="revision_needed"?"ต้องแก้ไข":`สถานะ: ${item.statusRaw}`; }
export function FolioCard({ item }:{item:ResearchViewModel}) { return <article className="folio"><div className="folio-head"><ArchiveTab>{item.category}</ArchiveTab><span className="mono muted">[ {item.ref} ]</span></div><h2><Link prefetch={false} href={`/research/${item.id}`}>{item.titleTh}</Link></h2><em className="latin">{item.titleEn}</em><p className="muted">{item.abstract}</p><div className="folio-meta"><Status tone={statusTone(item.status)}>{statusLabel(item)}</Status><span>ปีการศึกษา: <b>{item.year}</b></span><span className="latin">◉ {item.views}　⇩ {item.downloads}</span></div><span className="citation-line" aria-hidden="true"/></article>; }
export function ResearchRow({ item }:{item:ResearchViewModel}) { return <article className="research-row"><div><span className="mono">{item.ref}</span><div style={{marginTop:7}}><Status tone={statusTone(item.status)}>{statusLabel(item)}</Status></div></div><div><h3 style={{margin:0,fontSize:21}}><Link prefetch={false} href={`/research/${item.id}`}>{item.titleTh}</Link></h3><span className="muted">สร้างเมื่อ: {item.created} • {item.category}</span></div><span className="latin muted">◉ {item.views}　⇩ {item.downloads}</span></article>; }
